[project]
name = "book-api"
version = "0.1.0"
description = "Simple FastAPI + SQLAlchemy book service"
requires-python = ">=3.10"

dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "sqlalchemy>=2.0.31",
    "pydantic>=2.8.0",
]

